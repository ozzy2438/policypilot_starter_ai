global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "policypilot-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:8000"]
        labels:
          app: "policypilot"
          environment: "development"
